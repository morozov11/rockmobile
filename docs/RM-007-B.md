# RM-007-B — local favourites and history

Status: **contract-remediated, verification blocked** (2026-08-25).

`PersonalDataStore` now serializes the portable RM-007-A v1 field names with UUID profile/record IDs and RFC-3339 timestamps. Legacy epoch-based RM-007-B data is backup-and-journal migrated. Invalid or unsupported data fails closed without rewriting the original bytes; writes check synchronous persistence and an explicit rollback restores the backup.

The UI provides per-station favourite toggles and compact offline entries for favourites/history. Playback and catalogue fallback remain unchanged. The code includes pure resolver tests for URL stability, approved legacy mapping, merged/split/removed/missing lifecycle behaviour, deduplication and retention.

The checksum-pinned bundled catalog now supplies active IDs, legacy mappings and tombstones to the personal resolver during startup. Merge chains resolve only to active IDs; split, removed, missing and unmapped legacy references remain quarantined without URL/name lookup or automatic split selection.

Verification is pending: Gradle wrapper access was blocked by `C:\Users\alex\.gradle\wrapper\dists\gradle-9.3.1-bin\23ovyewtku6u96viwx3xl3oks\gradle-9.3.1-bin.zip.lck`. No lint result is claimed.
