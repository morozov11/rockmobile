# RM-004-E — RockMobile shared catalog

Status: **basic schema-v1 integration completed; extended offline part blocked by missing input artifact** (2026-08-21).

## Delivered

- Bundled fallback now reads `assets/stations.v1.json`, the vendored release candidate from `C:\repos\rockcast-station-catalog`; it performs no build-time or startup download.
- The accepted pin is schema `1`, catalog `2026.08.2`, SHA-256 `3fa20dca94fc059bd433a47b9fba9bb6d5e5e1aa2957a5ffb58b2a7b20b1d74d`.
- The loader rejects an invalid checksum/version/schema, empty catalog, duplicate canonical/legacy/stream IDs or URLs, and anything other than exactly one primary stream. Unknown optional v1 fields are ignored.
- Domain playback remains on the existing `streamUrl` field, now selected from the explicit primary stream. Full stream metadata is retained in `Station.streams`.
- RockServer remains first. Timeout/network/HTTP/malformed/empty remote responses use the bundled snapshot; playback errors stay isolated from catalog loading.
- Approved `rockmobile:rockcast-…` mappings migrate persisted unavailable-voice IDs atomically and idempotently. Unmatched old values are retained for this transition release.

## Deliberately deferred / acceptance gate

The only blocker for the extended RM-004-E part is the absence of the approved prebuilt extended SQLite/Room artifact exported by RM-004-D. The available release candidate is the independent curated 41-station baseline, not a ~16k export. This task does not invent stations, generate PostgreSQL exports, add Room against a nonexistent artifact, or represent the baseline as an extended catalog. Once RockServer supplies the versioned, checksummed SQLite artifact and its exact manifest/compatibility contract, the next task should vendor it, validate it independently, add indexed local name/tag search, and retain this baseline as final fallback.
